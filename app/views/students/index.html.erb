<div class="container justify-content-center my-5">
  <h1 class="container text-center text-decoration-underline my-5"><i>All Students</i></h1>
  <table class="table table-bordered container table-hover table-striped text-center">
    <thead class="table-dark">
      <tr>
        <th scope="col">First Name</th>
        <th scope="col">Last Name</th>
        <th scope="col">Date of Birth</th>
        <th scope="col">Department</th>
        <th scope="col">Show</th>
        <th scope="col">Edit</th>
        <th scope="col">Delete</th>
      </tr>
    </thead>
    <tbody>
      <% @students.each do |student| %>
        <tr>
          <td><%= student.first_name %></td>
          <td><%= student.last_name %></td>
          <td><%= student.date_of_birth %></td>
          <td><%= student.department %></td>
          <td><%= link_to "Show", student_path(student), :class => "btn btn-info" %></td>
          <td><%= link_to "Edit", edit_student_path(student), :class => "btn btn-secondary" %></td>
          <td><%= link_to "Delete", student_path(student), :class => "btn btn-danger", data: {
              turbo_method: :delete,
              turbo_confirm: :"Are You Sure?"
          } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <a href="/students/new"><input class="btn btn-dark my-3" type="submit" value="Add Student"></a>
</div>
