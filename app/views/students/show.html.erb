<div class="container justify-content-center my-5">
  <h1 class="container text-center text-decoration-underline my-5">Student Id - <%= @student.id %></h1>
  <table class="table table-bordered container table-hover table-striped">
  
    <tbody>
      <tr>
        <th scope="row">Name</th>
        <td><%= @student.first_name %></td>
      </tr>
      
      <tr>
        <th scope="row">Surname</th>
        <td><%= @student.last_name %></td>
      </tr>

      <tr>
        <th scope="row">Date of Birth</th>
        <td><%= @student.date_of_birth %></td>
      </tr>

      <tr>
        <th scope="row">Department</th>
        <td><%= @student.department %></td>
      </tr>
    </tbody>
  </table>

  <div class="my-4">
    <%= link_to "Edit", edit_student_path(@student), :class => 'btn btn-dark px-4' %>
    <%= link_to "Delete", student_path(@student), :class => 'btn btn-dark ms-3', data: {
      turbo_method: :delete,
      turbo_confirm: :"Are You Sure?"
    } %>
  </div>
</div>
